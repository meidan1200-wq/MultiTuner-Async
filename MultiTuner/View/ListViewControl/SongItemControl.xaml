<local:BaseListControl x:Class="MultiTuner.View.ListViewControl.SongItemControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MultiTuner.View.ListViewControl"
             xmlns:dd="clr-namespace:GongSolutions.Wpf.DragDrop;assembly=GongSolutions.Wpf.DragDrop"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ListBox x:Name="SongList"
         ItemsSource="{Binding Items, RelativeSource={RelativeSource AncestorType=local:BaseListControl}}"
         SelectionMode="Extended"
         PreviewMouseDown="SongList_PreviewMouseDown"        
         Background="Transparent"
         BorderThickness="0"
         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
         dd:DragDrop.IsDragSource="True"
         dd:DragDrop.IsDropTarget="True"
         dd:DragDrop.UseDefaultDragAdorner="True"
         dd:DragDrop.DropTargetAdornerBrush="LimeGreen">
            <!-- Remove default ListBoxItem padding -->
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Style.Triggers>
                        <!-- Hover -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#22FFFFFF"/>
                        </Trigger>
                        <!-- Selected -->
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#4466AFFF"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border CornerRadius="10" Padding="8">
                        <StackPanel Orientation="Horizontal"
                    VerticalAlignment="Center"
                    FlowDirection="LeftToRight">
                            <!-- SONG IMAGE -->
                            <Image Source="{Binding ThumbnailUrl}"
                   Width="60"
                   Height="60"
                   Stretch="Fill"
                   Margin="0,0,10,0"/>
                            <!-- SONG DETAILS -->
                            <StackPanel Orientation="Vertical"
                        VerticalAlignment="Center"
                        Margin="0,0,10,0">
                                <TextBlock Text="{Binding Title}"
                           Foreground="Silver"
                           FontSize="18"
                           FontFamily="Segoe UI Semibold"
                           FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding Artist}"
                           Foreground="LightGray"
                           FontSize="14"
                           FontFamily="Segoe UI"/>
                            </StackPanel>
                            <!-- DURATION -->
                            <TextBlock Text="{Binding Duration}"
                       Foreground="Silver"
                       FontSize="14"
                       VerticalAlignment="Center"
                       Margin="0,0,10,0"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</local:BaseListControl>